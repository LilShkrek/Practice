#include <iostream>
#include "Queue.h"

using namespace std;

int main() {

	setlocale(LC_ALL, "Russian");
	queue_t q;
	/*queue_t q;
	int x;
	while (cin.peek() != '\n') {

		cin >> x;
		Enqueue(q, x);

	}

	Destroy(q);*/

	temp();

}